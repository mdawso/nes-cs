Controls: 
- Z, X = A, B
- Enter, RShift = Start, Select
- Arrow Keys = DPad

I wrote this emulator in order to learn C# and .NET stuff, as well as more about the NES hardware. You should use a more mature one to actually play games.
